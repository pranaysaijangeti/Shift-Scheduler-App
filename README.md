# Shift-Scheduler-App
Cross-platform React Native app (Android/iOS) with MongoDB backend. Upload shift schedules (images/text/Excel), extract via OCR, store user shifts, and auto-set calendar events + alarms with custom reminders. Node.js API, offline sync via Realm.
